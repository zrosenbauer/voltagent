{
  "$schema": "https://json-schema.org/schema",
  "$id": "Example",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Provide a name in kebab-case for the example, this will be used to generate the directory name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use? (kebab-case)"
    },
    "description": {
      "type": "string",
      "description": "The description of the example, this will be used to generate the README",
      "x-prompt": "What description would you like to use?"
    }
  },
  "required": ["name", "description"]
}
