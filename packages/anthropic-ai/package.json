{
  "name": "@voltagent/anthropic-ai",
  "version": "0.1.3",
  "description": "Anthropic AI provider for VoltAgent",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "dist/*.map"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "dts": "tsc --emitDeclarationOnly --outDir dist --declaration --traceResolution",
    "lint": "tsup -v",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@voltagent/core": "0.1.14",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.24",
    "eslint": "^8.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "tsup": "^6.7.0",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "@voltagent/core": "^0.1.0"
  }
}
