import type {
  ReadableStreamType,
  Voice,
  VoiceEventData,
  VoiceEventType,
  VoiceMetadata,
  VoiceOptions,
} from "@voltagent/core";

// Re-export all voice types from core
export type {
  Voice,
  VoiceEventData,
  VoiceEventType,
  VoiceMetadata,
  VoiceOptions,
  ReadableStreamType,
};
