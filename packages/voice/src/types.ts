import type {
  Voice,
  VoiceEventData,
  VoiceEventType,
  VoiceMetadata,
  VoiceOptions,
  ReadableStreamType,
} from "@voltagent/core";

// Re-export all voice types from core
export type {
  Voice,
  VoiceEventData,
  VoiceEventType,
  VoiceMetadata,
  VoiceOptions,
  ReadableStreamType,
};
